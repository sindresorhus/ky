import test from 'ava';

test('stream - fail on unsupported browsers', t => {
	t.pass();
});

test('stream - successfully fetch stream', t => {
	t.pass();
});

test('stream - pass content length and download % progress to streamProgressCallback', t => {
	t.pass();
});

test('stream - pass percent null if no content-length header', t => {
	t.pass();
});
